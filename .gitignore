# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/

# NOTE: next-env.d.ts is deliberately committed. It is generated by `next build`,
# but CI runs `typecheck` before `build`, so tsc needs it present in a fresh
# checkout. Next's docs also say to commit it.

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# testing / coverage
coverage/

# editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp

# misc
*.tsbuildinfo
.eslintcache
.vercel
